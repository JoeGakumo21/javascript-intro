let age=parseInt(prompt("How old are u"));
age=age+90;
// finding area of a circle
const pi=3.1456;
let radius=parseFloat(prompt("enter any value for radius"));
let area=(pi*Math.pow(radius,2)).toExponential(3)
let sqrt=Math.sqrt(area);
let area2=sqrt**2;
